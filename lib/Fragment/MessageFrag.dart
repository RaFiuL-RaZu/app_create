import 'package:flutter/cupertino.dart';

class MessageFrag extends StatelessWidget{
  @override
  Widget build(BuildContext context) {

    return Container(
      child: Center(
        child: Text("MessageFragment"),
      ),
    );
  }
}