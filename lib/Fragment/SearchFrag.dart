import 'package:flutter/cupertino.dart';

class SearchFrag extends StatelessWidget{
  @override
  Widget build(BuildContext context) {

    return Container(
      child: Center(
        child: Text("SeacrchFragment"),
      ),
    );
  }
}